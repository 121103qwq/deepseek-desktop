# DeepSeek Desktop

Windows x64 installer source for a community DeepSeek Harness desktop distribution.

The installer opens an embedded `DeepSeek Desktop` WebView, lets the user choose Groq Free Plan or DeepSeek API at startup, and publishes both standard and full offline setup executables.

See [the Windows package documentation](distribution/windows/README.md) for build and verification instructions.
